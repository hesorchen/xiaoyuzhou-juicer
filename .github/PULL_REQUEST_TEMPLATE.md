<!-- 简述这个 PR 做了什么、为什么。 -->

## 改动内容

-

## 自检清单

- [ ] `ruff check scripts/` 通过
- [ ] `python3 -m compileall -q scripts` 通过
- [ ] **未提交任何 token / 凭证**（`git diff --cached --name-only | grep -i token` 无输出）
- [ ] 面向用户的改动已更新 `CHANGELOG.md`
- [ ] 接口/参数改动已同步 `README.md` 与 `SKILL.md`
