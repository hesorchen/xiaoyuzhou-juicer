[archive]
# 复制为 config/archive.ini 后填写。支持 ~ 和环境变量。
dir = ~/notes/podcasts

# 可用变量：date、podcast、episode、title、topic、kind。
# 不要在模板末尾写 .md，脚本会自动添加。
filename_pattern = {date}-{podcast}-{episode}-{topic}-{kind}
